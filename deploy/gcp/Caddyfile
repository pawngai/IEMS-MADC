34.67.90.71.sslip.io {
    encode gzip zstd

    @backend path /api/* /health/* /openapi.json /docs* /redoc*
    handle @backend {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:80
    }
}