"""Reserved namespace.

Profile services live under ``contexts.employee_profile.application.services``.
This package is kept empty to satisfy the bounded-context topology guard
(``test_required_context_subpackages_exist``) and as the future home for any
profile services that don't belong in the application layer.
"""
